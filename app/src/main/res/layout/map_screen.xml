<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:id="@+id/map_container"
             android:layout_width="match_parent"
             android:layout_height="match_parent">

    <!-- Map container fills the screen -->



    <!-- Radio UI overlay in the middle -->
    <LinearLayout
            android:id="@+id/mid_panel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"

            android:layout_gravity="center"
            android:elevation="8dp">
        <!-- Radio layout will be inflated here dynamically -->
    </LinearLayout>

    <!-- Compass button (optional overlay) -->
    <ImageButton
            android:id="@+id/btnCompassToggle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_margin="12dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_compass"
            android:contentDescription="Toggle compass"
            android:layout_gravity="top|end"
            android:focusable="true"
            android:clickable="true"/>

</FrameLayout>
